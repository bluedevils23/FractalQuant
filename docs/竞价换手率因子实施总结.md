# 竞价换手率因子实施总结

## 实施日期
2026-09-05

## 因子定义

### 1. auction_turnover_rate（总股本换手率）
```
auction_turnover_rate = (集合竞价成交量 / 总股本) × 100
```

**说明**：
- 集合竞价成交量：09:25:00 实际成交的股数
- 总股本（float_share）：前一交易日的总流通股本
- 单位：百分比（%）

### 2. auction_turnover_rate_free（流通股本换手率）
```
auction_turnover_rate_free = (集合竞价成交量 / 流通股本) × 100
```

**说明**：
- 流通股本（free_share）：前一交易日的流通股本（不含限售）
- 通常 free_share ≤ float_share
- 因此 auction_turnover_rate_free ≥ auction_turnover_rate

## 实施内容

### 1. 数据加载（build_historical_context）

**文件**: `FractalQuant/scripts/generate_auction_factors.py:1938`

**修改**:
```python
# 添加到 optional_columns
optional_columns = ["vol", "circ_mv", "up_limit", "down_limit", "float_share", "free_share"]

# 添加字段处理
work["previous_day_float_share"] = work["float_share"].where(work["float_share"].gt(0))
work["previous_day_free_share"] = work["free_share"].where(work["free_share"].gt(0))

# 添加到 context_columns
context_columns = [
    "trade_date",
    "ts_code",
    "previous_day_volume_shares",
    "previous_day_high",
    "previous_7d_close_max",
    "previous_day_float_market_cap_cny",
    "previous_day_float_share",      # 新增
    "previous_day_free_share",        # 新增
]
```

### 2. 上下文传递（apply_supplemental_context）

**文件**: `FractalQuant/scripts/generate_auction_factors.py:2317`

**修改**:
```python
context_columns = [
    "previous_day_volume_shares",
    "previous_day_high",
    "previous_7d_close_max",
    "previous_day_float_market_cap_cny",
    "previous_day_float_share",       # 新增
    "previous_day_free_share",         # 新增
    "auction_limit_up_price",
    "auction_limit_down_price",
]
```

### 3. 因子计算函数

**文件**: `FractalQuant/scripts/generate_auction_factors.py:2287`

**新增函数**:
```python
def _apply_turnover_rate_factors(result: pd.DataFrame, index: int) -> None:
    """Calculate auction turnover rate factors.

    auction_turnover_rate = auction_matched_volume / float_share * 100
    auction_turnover_rate_free = auction_matched_volume / free_share * 100
    """
    auction_volume = result.at[index, "auction_matched_volume"]
    float_share = result.at[index, "previous_day_float_share"]
    free_share = result.at[index, "previous_day_free_share"]

    # Initialize columns
    result.at[index, "auction_turnover_rate"] = np.nan
    result.at[index, "auction_turnover_rate_free"] = np.nan

    if not np.isfinite(auction_volume) or auction_volume <= 0:
        return

    # Calculate turnover rate using float_share
    if np.isfinite(float_share) and float_share > 0:
        result.at[index, "auction_turnover_rate"] = float(
            auction_volume / float_share * 100.0
        )

    # Calculate turnover rate using free_share
    if np.isfinite(free_share) and free_share > 0:
        result.at[index, "auction_turnover_rate_free"] = float(
            auction_volume / free_share * 100.0
        )
```

### 4. 因子列定义

**文件**: `FractalQuant/scripts/generate_auction_factors.py:218`

```python
TURNOVER_FACTOR_COLUMNS = [
    "auction_turnover_rate",
    "auction_turnover_rate_free",
]
```

**补充引用列**:
```python
SUPPLEMENT_REFERENCE_COLUMNS = [
    "auction_stage1_end_time",
    "auction_stage2_end_time",
    "previous_day_volume_shares",
    "previous_day_high",
    "previous_7d_close_max",
    "previous_day_float_market_cap_cny",
    "previous_day_float_share",      # 新增
    "previous_day_free_share",        # 新增
    "auction_limit_up_price",
    "auction_limit_down_price",
]
```

### 5. 因子调用

**文件**: `FractalQuant/scripts/generate_auction_factors.py:2539`

```python
# Calculate turnover rate factors
for index, row in result.iterrows():
    _apply_turnover_rate_factors(result, index)
```

## 测试验证

### 单元测试
**文件**: `test_turnover_rate_factors.py`

测试内容：
1. 基础计算逻辑
2. 边界情况（缺失数据、零值）
3. 合理值范围（0-10%）

测试结果：**全部通过** ✓

### 真实数据测试
**文件**: `test_real_turnover_rate.py`

测试数据：
- 日期：2026-01-15
- 股票：000001.SZ（平安银行）
- 集合竞价成交量：127,027,500 股
- 总股本：19,405,600,653 股
- 流通股本：8,160,481,215 股

计算结果：
- `auction_turnover_rate` = 0.6546%
- `auction_turnover_rate_free` = 1.5566%

验证通过：
- ✓ 换手率在合理范围内（0.1%-5%）
- ✓ 流通股换手率 >= 总股换手率
- ✓ 与实际成交数据一致

## 数据源

### 股本数据
- **来源**: `D:/workspace/stockdata/stock-data/行情数据/stock_daily.parquet`
- **字段**:
  - `float_share`: 总股本（单位：万股）
  - `free_share`: 流通股本（单位：万股）
- **覆盖率**: 14.5M 记录，覆盖度良好

### 成交数据
- **来源**: 逐笔成交数据 `E:/逐笔数据/YYYY/YYYYMM/YYYYMMDD/{symbol}/逐笔成交.csv`
- **关键字段**:
  - `时间`: 成交时间（格式：9HMMSSmmm 或 HHMMSSmmm）
  - `成交数量`: 成交量（单位：手）
  - `成交代码`: 0=成交，C=撤单

### 集合竞价成交时间
- **上海**: 09:15-09:25 连续撮合
- **深圳**: 09:25:00 集中撮合
  - 测试代码过滤：`hour==9 AND minute==25 AND 成交代码==0`

## 数据处理细节

### 1. 单位转换
```python
# float_share: 万股 → 股
float_share_in_shares = float_share * 10000

# 成交量: 手 → 股
volume_in_shares = volume_in_lots * 100
```

### 2. 时间解析（深圳）
```python
def parse_auction_time(time_val):
    s = str(int(time_val))
    if len(s) == 8:
        if s[0] == '9':  # 9HMMSSmmm
            return 9, int(s[1:3])
        else:  # HHMMSSmmm
            return int(s[0:2]), int(s[2:4])
    return None, None
```

示例：
- `91500060` → 09:15:00.060
- `92500000` → 09:25:00.000
- `143002500` → 14:30:02.500

### 3. 缺失值处理
- 成交量 ≤ 0 → NaN
- float_share / free_share 缺失或 ≤ 0 → NaN
- 保留一个字段有效即可输出部分结果

## 预期结果范围

### 典型值
- **一般股票**: 0.5% - 2.0%
- **活跃股票**: 2.0% - 5.0%
- **极端情况**: > 5.0%（需关注）

### 关系
- `auction_turnover_rate_free` ≥ `auction_turnover_rate`
- 比值 = `float_share` / `free_share`（通常 1.0 - 3.0）

## 已删除的旧代码

### 删除的函数
1. `_apply_turnover_factors()` - 旧的实现，使用了不同的计算逻辑
2. `_apply_turnover_ratio_averages()` - 计算5日/20日平均的函数

### 删除的因子列
```python
# 旧定义（已删除）
TURNOVER_FACTOR_COLUMNS = [
    "auction_turnover_ratio",
    "auction_turnover_ratio_5d",
    "auction_turnover_ratio_20d",
]
```

## 后续建议

### 1. 可选扩展因子
- `auction_turnover_ratio_5d`: 5日平均换手率
- `auction_turnover_ratio_20d`: 20日平均换手率
- `auction_turnover_zscore`: Z-score 标准化

### 2. 数据质量监控
- 检查 float_share 和 free_share 的缺失率
- 监控换手率异常值（> 10%）
- 验证深圳股票09:25:00成交记录完整性

### 3. 性能优化
- 当前实现为逐行计算（row-by-row）
- 可考虑向量化计算提升性能

## 相关文档
- [竞价换手率因子实现计划.md](竞价换手率因子实现计划.md)
- [集合竞价因子实施总结.md](集合竞价因子实施总结.md)
- [上海深圳逐笔数据差异分析.md](上海深圳逐笔数据差异分析.md)

## 实施人员
Claude Code (Fable 5)

## 状态
✅ **已完成** - 所有测试通过，代码已集成
